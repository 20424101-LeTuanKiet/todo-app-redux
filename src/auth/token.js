// import API from '../utils/XMLHttpRequests';

export default function token(accessToken, refreshToken, method, path, body) {
    switch (method) {
        case 'post':
            break;
        default:
            break;
    }
}
